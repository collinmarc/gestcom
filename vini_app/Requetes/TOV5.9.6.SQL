
/* Pour éviter les problèmes éventuels de perte de données, passez attentivement ce script en revue avant de l'exécuter en dehors du contexte du Concepteur de bases de données.*/
BEGIN TRANSACTION
SET QUOTED_IDENTIFIER ON
SET ARITHABORT ON
SET NUMERIC_ROUNDABORT OFF
SET CONCAT_NULL_YIELDS_NULL ON
SET ANSI_NULLS ON
SET ANSI_PADDING ON
SET ANSI_WARNINGS ON
COMMIT
BEGIN TRANSACTION
GO
ALTER TABLE dbo.CONSTANTES ADD
	CST_FTPEDI_SRV varchar(MAX) NULL,
	CST_FTPEDI_USER varchar(MAX) NULL,
	CST_FTPEDI_PWD varchar(MAX) NULL,
	CST_FTPEDI_PORT varchar(MAX) NULL,
	CST_FTPEDI_REP varchar(MAX) NULL,
	CST_FTPEDI_REPLOCAL varchar(MAX) NULL
GO
ALTER TABLE dbo.FOURNISSEUR ADD
	FRN_BINTERMEDIAIRE int NULL,
	FRN_DOSSIER nvarchar(MAX) NULL
GO
ALTER TABLE dbo.FOURNISSEUR SET (LOCK_ESCALATION = TABLE)
GO 
ALTER TABLE dbo.CONSTANTES SET (LOCK_ESCALATION = TABLE)
GO
UPDATE FOURNISSEUR SET FRN_BINTERMEDIAIRE = 0, FRN_DOSSIER='';
COMMIT

--Numéro de version
UPDATE CONSTANTES SET CST_VERSION_BD='5.9.6';